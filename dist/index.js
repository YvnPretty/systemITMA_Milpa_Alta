// Archivo principal de entrada (src/index.js)
import express from 'express';
import axios from 'axios';
import _ from 'lodash';
console.log('🚀 Proyecto "plantilla" iniciado correctamente desde src/index.js');

// Ejemplo de uso de lodash:
const numeros = [1, 2, 3, 4, 5];
console.log('Suma con lodash:', _.sum(numeros));